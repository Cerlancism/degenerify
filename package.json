{
  "name": "degenerify",
  "description": "A NodeJS server, also dubbed as a CORS Proxy and serve a page to degerify a target webpage.",
  "version": "0.0.1",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.16.4",
    "request": "^2.88.0"
  }
}
